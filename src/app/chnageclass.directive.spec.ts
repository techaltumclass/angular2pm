import { ChnageclassDirective } from './chnageclass.directive';

describe('ChnageclassDirective', () => {
  it('should create an instance', () => {
    const directive = new ChnageclassDirective();
    expect(directive).toBeTruthy();
  });
});
